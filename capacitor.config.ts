import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.com.competitivepestservices.quote',
  appName: 'Competitive Pest Services',
  webDir: 'dist'
};

export default config;
